{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "A topological directed edge connecting two buses. Arcs are used to define the `from_id` and `to_id` endpoints when defining a line or transformer.",
  "properties": {
    "id": {
      "description": "Unique integer identifier for this component.",
      "type": "integer"
    },
    "from_id": {
      "description": "ID of the initial bus.",
      "type": "integer"
    },
    "to_id": {
      "description": "ID of the terminal bus.",
      "type": "integer"
    }
  },
  "required": [
    "id",
    "from_id",
    "to_id"
  ],
  "title": "Arc",
  "type": "object"
}
