{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Facts control devices.\n\nMost often used in AC power flow studies as a control of voltage and, active and reactive power.",
  "properties": {
    "name": {
      "description": "Name of the component. Components of the same type (e.g., `PowerLoad`) must have unique names, but components of different types (e.g., `PowerLoad` and `ACBus`) can have the same name.",
      "type": "string"
    },
    "id": {
      "description": "Unique integer identifier for this component.",
      "type": "integer"
    },
    "available": {
      "description": "Indicator of whether the component is connected and online (`true`) or disconnected, offline, or down (`false`). Unavailable components are excluded during simulations.",
      "type": "boolean"
    },
    "bus": {
      "description": "Sending end bus number.",
      "type": "integer"
    },
    "control_mode": {
      "description": "Control mode. Used to describe the behavior of the control device.",
      "$ref": "../../Operations/common.json#/$defs/FACTSControlDeviceControlMode"
    },
    "voltage_setpoint_units": {
      "description": "Unit basis for voltage_setpoint. COMPONENT_BASE (pu on the bus base voltage) is PSS/E RAW native (VSET).",
      "default": "COMPONENT_BASE",
      "$ref": "../../Operations/common.json#/$defs/VoltageUnitBasis"
    },
    "voltage_setpoint": {
      "description": "Voltage setpoint at the sending end bus in kV, it has to be a `PV` bus. Units: kV. Units: per voltage_setpoint_units — NATURAL_UNITS: kV, COMPONENT_BASE: pu .",
      "type": "number",
      "x-unit-discriminator": "voltage_setpoint_units",
      "x-units": {
        "NATURAL_UNITS": "kV",
        "COMPONENT_BASE": "pu"
      },
      "x-quantity": "Voltage"
    },
    "max_shunt_current": {
      "description": "Maximum shunt current at the sending end bus; entered at unity voltage. Units: per power_units — NATURAL_UNITS: MVA, COMPONENT_BASE: pu .",
      "type": "number",
      "x-unit-discriminator": "power_units",
      "x-units": {
        "NATURAL_UNITS": "MVA",
        "COMPONENT_BASE": "pu"
      },
      "x-quantity": "ApparentPower"
    },
    "reactive_power_required": {
      "description": "Total reactive power required to hold voltage at sending bus, as a fraction in the range 0-1. Units: 1.",
      "type": "number",
      "x-unit": "1",
      "x-quantity": "Fraction"
    },
    "max_reactive_power": {
      "description": "Independent maximum reactive power ceiling; the device reactive limit is min(the current/susceptance law on max_shunt_current, this value). Non-binding at the 9999.0 default. Units: per power_units — NATURAL_UNITS: MVAr, COMPONENT_BASE: pu .",
      "default": 9999.0,
      "type": "number",
      "x-unit-discriminator": "power_units",
      "x-units": {
        "NATURAL_UNITS": "MVAr",
        "COMPONENT_BASE": "pu"
      },
      "x-quantity": "ReactivePower"
    },
    "shunt_control_type": {
      "description": "Device class selecting the reactive-limit law (SVC vs STATCOM).",
      "default": "STATCOM",
      "type": "string",
      "enum": [
        "SVC",
        "STATCOM"
      ]
    },
    "regulated_bus_number": {
      "description": "Bus whose voltage this device regulates; 0 means local (sending) bus (PSS/E FCREG). Units: 1.",
      "default": 0,
      "type": "integer",
      "x-unit": "1",
      "x-quantity": "Dimensionless"
    },
    "base_power": {
      "description": "System base power for per-unitization of this component's per-unit fields, recorded per component in lieu of a system-level table. Units: MVA.",
      "type": "number",
      "x-unit": "MVA"
    },
    "power_units": {
      "description": "Unit basis for this component's power-family fields (active/reactive/apparent power, ratings, limits, ramp rates). COMPONENT_BASE: per unit on this component's own base_power. NATURAL_UNITS: the field's physical unit.",
      "$ref": "../../Core/common.json#/$defs/UnitSystem"
    },
    "dynamic_injector": {
      "description": "ID of the corresponding dynamic injection model for FACTS control device, if any.",
      "type": [
        "integer",
        "null"
      ]
    }
  },
  "required": [
    "name",
    "id",
    "available",
    "bus",
    "max_shunt_current",
    "reactive_power_required",
    "voltage_setpoint",
    "base_power",
    "power_units"
  ],
  "title": "FACTSControlDevice",
  "type": "object"
}
