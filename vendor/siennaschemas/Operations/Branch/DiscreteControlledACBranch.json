{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Used to represent switches and breakers connecting AC Buses.",
  "properties": {
    "id": {
      "description": "Unique integer identifier for this component.",
      "type": "integer"
    },
    "name": {
      "description": "Name of the component. Components of the same type (e.g., `PowerLoad`) must have unique names, but components of different types (e.g., `PowerLoad` and `ACBus`) can have the same name.",
      "type": "string"
    },
    "available": {
      "description": "Indicator of whether the component is connected and online (`true`) or disconnected, offline, or down (`false`). Unavailable components are excluded during simulations.",
      "type": "boolean"
    },
    "active_power_flow": {
      "description": "Initial condition of active power flow on the line. Units: per power_units — NATURAL_UNITS: MW, COMPONENT_BASE: pu .",
      "type": "number",
      "x-unit-discriminator": "power_units",
      "x-units": {
        "NATURAL_UNITS": "MW",
        "COMPONENT_BASE": "pu"
      },
      "x-quantity": "ActivePower"
    },
    "reactive_power_flow": {
      "description": "Initial condition of reactive power flow on the line. Units: per power_units — NATURAL_UNITS: MVAr, COMPONENT_BASE: pu .",
      "type": "number",
      "x-unit-discriminator": "power_units",
      "x-units": {
        "NATURAL_UNITS": "MVAr",
        "COMPONENT_BASE": "pu"
      },
      "x-quantity": "ReactivePower"
    },
    "arc": {
      "description": "An `Arc` defining this line `from` a bus `to` another bus.",
      "type": "integer"
    },
    "base_power": {
      "description": "System base power for per-unitization of this component's per-unit fields, recorded per component in lieu of a system-level table. Units: MVA.",
      "type": "number",
      "x-unit": "MVA"
    },
    "power_units": {
      "description": "Unit basis for this component's power-family fields (active/reactive/apparent power, ratings, limits, ramp rates). COMPONENT_BASE: per unit on this component's own base_power. NATURAL_UNITS: the field's physical unit.",
      "$ref": "../../Core/common.json#/$defs/UnitSystem"
    },
    "r": {
      "description": "Resistance. Per-unit on `base_power`, which records the system base. Units: pu.",
      "type": "number",
      "x-unit": "pu",
      "x-quantity": "Resistance"
    },
    "x": {
      "description": "Reactance. Per-unit on `base_power`, which records the system base. Units: pu.",
      "type": "number",
      "x-unit": "pu",
      "x-quantity": "Reactance"
    },
    "rating": {
      "description": "Thermal rating. Flow on the branch must be between -`rating` and `rating`. Units: per power_units — NATURAL_UNITS: MVA, COMPONENT_BASE: pu .",
      "type": "number",
      "x-unit-discriminator": "power_units",
      "x-units": {
        "NATURAL_UNITS": "MVA",
        "COMPONENT_BASE": "pu"
      },
      "x-quantity": "ApparentPower"
    },
    "discrete_branch_type": {
      "description": "Type of discrete control.",
      "default": "OTHER",
      "type": "string",
      "enum": [
        "SWITCH",
        "BREAKER",
        "OTHER"
      ]
    },
    "branch_status": {
      "description": "Open or Close status.",
      "default": "CLOSED",
      "type": "string",
      "enum": [
        "OPEN",
        "CLOSED"
      ]
    },
    "normal_branch_status": {
      "description": "Normal (as-designed) open or close status of the device.",
      "default": "CLOSED",
      "type": "string",
      "enum": [
        "OPEN",
        "CLOSED"
      ]
    }
  },
  "required": [
    "id",
    "name",
    "available",
    "active_power_flow",
    "reactive_power_flow",
    "arc",
    "r",
    "x",
    "base_power",
    "power_units",
    "rating"
  ],
  "title": "DiscreteControlledACBranch",
  "type": "object"
}
