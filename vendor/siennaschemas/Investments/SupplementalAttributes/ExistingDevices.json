{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Supplemental attribute mapping a technology in the portfolio to the existing system — for example, the list of existing generators that correspond to one supply technology.",
  "properties": {
    "id": {
      "description": "ID for individual component.",
      "type": "integer"
    },
    "existing_devices": {
      "description": "List of individual existing devices to map to a specific technology in the portfolio.",
      "type": "array",
      "items": {
        "type": "string"
      }
    }
  },
  "required": [
    "id"
  ],
  "title": "ExistingDevices",
  "type": "object"
}
