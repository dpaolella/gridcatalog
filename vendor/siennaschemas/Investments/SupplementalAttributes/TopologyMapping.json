{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Supplemental attribute storing the mapping between a zone and the associated buses in the base system.",
  "properties": {
    "id": {
      "description": "ID for individual component.",
      "type": "integer"
    },
    "buses": {
      "description": "List of buses in the base system that are associated with a zone.",
      "type": "array",
      "items": {
        "type": "string"
      }
    }
  },
  "required": [
    "id"
  ],
  "title": "TopologyMapping",
  "type": "object"
}
