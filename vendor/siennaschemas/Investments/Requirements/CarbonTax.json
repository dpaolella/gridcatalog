{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Policy requirement defining an additional cost penalty per ton of CO2 produced in the eligible regions during the target year.",
  "properties": {
    "id": {
      "description": "ID for individual component.",
      "type": "integer"
    },
    "name": {
      "description": "Name of the component.",
      "type": "string"
    },
    "available": {
      "description": "Indicator of whether the component is connected and online (`true`) or disconnected, offline, or down (`false`).",
      "type": "boolean"
    },
    "target_year": {
      "description": "Year in which this requirement is applied.",
      "type": "integer"
    },
    "tax_dollars_per_ton": {
      "description": "Cost penalty per ton of CO2 emitted by technologies in the eligible regions during the target year. Units: USD/t.",
      "type": "number",
      "default": 0.0,
      "x-unit": "USD/t"
    }
  },
  "required": [
    "name",
    "id",
    "available"
  ],
  "title": "CarbonTax",
  "type": "object"
}
