{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Policy requirement that the total generation of the eligible technologies be at least a pre-determined fraction of the total annual demand across the eligible zones in the target year.",
  "properties": {
    "id": {
      "description": "ID for individual component.",
      "type": "integer"
    },
    "name": {
      "description": "Name of the component.",
      "type": "string"
    },
    "available": {
      "description": "Indicator of whether the component is connected and online (`true`) or disconnected, offline, or down (`false`).",
      "type": "boolean"
    },
    "target_year": {
      "description": "Year in which this requirement is applied.",
      "type": "integer"
    },
    "generation_fraction_requirement": {
      "description": "Fraction of total annual demand across all eligible zones that needs to be met by eligible resources. Units: 1.",
      "type": "number",
      "default": 0.0,
      "x-unit": "1",
      "x-quantity": "Fraction"
    }
  },
  "required": [
    "name",
    "id",
    "available"
  ],
  "title": "EnergyShareRequirements",
  "type": "object"
}
