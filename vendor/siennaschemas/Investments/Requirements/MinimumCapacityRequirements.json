{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Policy requirement that the total capacity of all eligible technologies in the target year be greater than or equal to the specified minimum in MW.",
  "properties": {
    "id": {
      "description": "ID for individual component.",
      "type": "integer"
    },
    "name": {
      "description": "Name of the component.",
      "type": "string"
    },
    "available": {
      "description": "Indicator of whether the component is connected and online (`true`) or disconnected, offline, or down (`false`).",
      "type": "boolean"
    },
    "target_year": {
      "description": "Year in which this requirement is applied.",
      "type": "integer"
    },
    "min_capacity_mw": {
      "description": "Minimum total capacity across all eligible resources. Units: MW.",
      "type": "number",
      "x-unit": "MW"
    }
  },
  "required": [
    "name",
    "id",
    "available"
  ],
  "title": "MinimumCapacityRequirements",
  "type": "object"
}
