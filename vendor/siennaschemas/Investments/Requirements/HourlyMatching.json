{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Policy requirement that the qualified demand-side technologies have their demand met by an equal amount of generation from qualified supply in every hour, such that `sum(P)_qualified_supply >= sum(D)_qualified_demand`.",
  "properties": {
    "id": {
      "description": "ID for individual component.",
      "type": "integer"
    },
    "name": {
      "description": "Name of the component.",
      "type": "string"
    },
    "available": {
      "description": "Indicator of whether the component is connected and online (`true`) or disconnected, offline, or down (`false`).",
      "type": "boolean"
    }
  },
  "required": [
    "name",
    "id",
    "available"
  ],
  "title": "HourlyMatching",
  "type": "object"
}
